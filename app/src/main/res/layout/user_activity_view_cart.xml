<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".UsersActivity.ViewCart">

    <ListView
        android:id="@+id/listview_viewcart"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_above="@+id/tv_grandtotal"
        android:layout_marginBottom="5dp">

    </ListView>

    <TextView
        android:id="@+id/tv_grandtotal"
        android:layout_width="wrap_content"
        android:layout_height="28dp"
        android:layout_above="@id/button_payment"
        android:layout_alignParentEnd="true"
        android:layout_marginBottom="10dp"
        android:text="Grandtotal:- "
        android:textSize="20dp" />

    <Button
        android:id="@+id/button_payment"
        android:layout_width="match_parent"
        android:layout_height="50dp"
        android:layout_alignParentBottom="true"
        android:layout_marginLeft="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="11dp"
        android:background="@drawable/bg_button"
        android:onClick="nextToPayment"
        android:text="Next"
        android:textColor="#fff"
        android:textSize="23dp" />

</RelativeLayout>